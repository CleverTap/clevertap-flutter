<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:name=".MyApplication"
        android:label="example"
        android:icon="@mipmap/ic_launcher"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.example.clevertap_plugin_example.MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="io.flutter.embedding.android.SplashScreenDrawable"
            android:resource="@drawable/launch_background" />
        <meta-data
            android:name="io.flutter.embedding.android.NormalTheme"
            android:resource="@style/NormalTheme"
            />
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="CLEVERTAP_ACCOUNT_ID"
            android:value="YOUR_CLEVERTAP_ACCOUNT_ID" />
        <meta-data
            android:name="CLEVERTAP_TOKEN"
            android:value="YOUR_CLEVERTAP_TOKEN" />
        <meta-data
            android:name="CLEVERTAP_REGION"
            android:value="YOUR_CLEVERTAP_REGION"/>

        <meta-data
            android:name="CLEVERTAP_HANDSHAKE_DOMAIN"
            android:value="YOUR HANDSHAKE DOMAIN"/>

        <!-- IMPORTANT: To force use Google AD ID to uniquely identify  users, use the following meta tag. GDPR mandates that if you are using this tag, there is prominent disclousure to your end customer in their application. Read more about GDPR here - https://clevertap.com/blog/in-preparation-of-gdpr-compliance/ -->
        <meta-data
            android:name="CLEVERTAP_USE_GOOGLE_AD_ID"
            android:value="0"/>

        <meta-data
            android:name="CLEVERTAP_ENCRYPTION_IN_TRANSIT"
            android:value="0" />

        <!-- Huawei Push (HMS) provider registration -->
        <meta-data
            android:name="CLEVERTAP_PROVIDER_1"
            android:value="hps,hps_token,com.clevertap.android.hms.HmsPushProvider,com.huawei.hms.push.HmsMessageService" />

        <!-- Self-handled messages: huawei_push's FlutterHmsMessageService is the sole
             HMS message receiver, so incoming pushes reach Dart (onMessageReceivedStream)
             and are rendered via CleverTapPlugin.createNotification. Disable clevertap-hms's
             CTHmsMessageService (auto-merged from the add-on) to avoid a second receiver. -->
        <service
            android:name="com.clevertap.android.hms.CTHmsMessageService"
            tools:node="remove" />

        <!-- Required by huawei_push so background/killed-state messages reach the Dart
             background handler (huawei_push broadcasts to this receiver when the app is
             not in the foreground). Without it, onMessageReceivedStream works but the
             registerBackgroundMessageHandler callback never fires. -->
        <receiver
            android:name="com.huawei.hms.flutter.push.receiver.BackgroundMessageBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.huawei.hms.flutter.push.receiver.BACKGROUND_REMOTE_MESSAGE" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.clevertap.android.sdk.pushnotification.fcm.FcmMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <service
            android:name="com.clevertap.android.sdk.pushnotification.CTNotificationIntentService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.clevertap.PUSH_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
